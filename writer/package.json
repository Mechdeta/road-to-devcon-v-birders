{
  "name": "@deccan/writer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Deccan Birders — Writer application. Authenticate with Swarm ID, record sightings, upload to Swarm.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test src/**/*.test.js"
  },
  "dependencies": {
    "@deccan/sighting-format": "file:../shared",
    "@ethersphere/bee-js": "13.1.0",
    "@snaha/swarm-id": "^0.4.1"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
